name: data_managing_app
description: A new Flutter project.


publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'


dependencies:
  flutter:
    sdk: flutter
  
  # Core Backend
  data_managing_core:
    path: /Users/joshua/Documents/Projects/data_managing_core

  # State Management
  flutter_riverpod: ^2.3.6
  riverpod: ^2.3.6

  # UI/UX
  cupertino_icons: ^1.0.2
  responsive_sizer: ^3.1.1
  google_fonts: ^4.0.4
  flutter_staggered_animations: ^1.1.1
  shimmer_animation: ^2.1.0+1

  # Utilities
  go_router: ^7.0.0
  freezed_annotation: ^2.4.1
  shared_preferences: ^2.0.16

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  flutter_native_splash: ^2.2.9
  flutter_launcher_icons: ^0.13.1
  build_runner: ^2.3.3
  json_serializable: ^6.6.1
  copy_with_extension_gen: ^5.0.2
  freezed: ^2.3.2

flutter_native_splash:
  image: "assets/dma_icon.png"
  color: "#204B6E"
  android_12:
    image: "assets/dma_icon.png"
    icon_background_color: "#204B6E"
  web: false

flutter_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/dma_icon.png"
  web:
    generate: true
    image_path: "assets/dma_icon.png"
    background_color: "#h204B6E"
    theme_color: "#h204B6E"

flutter:
  assets:
    - assets/
  uses-material-design: true